# {{ project_name }} raw BIDS dataset

This directory is the raw BIDS dataset root for the outer project workspace.

Point BIDS tools, MNE-BIDS, and `paths.bids_root` here, not to the outer project
folder.

Original acquisition exports are stored separately in `../sourcedata/`.
Processed outputs are stored separately in `../derivatives/`.
